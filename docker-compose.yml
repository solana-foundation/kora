services:
  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
    restart: unless-stopped
    command: redis-server /usr/local/etc/redis/redis.conf

  kora:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./kora.toml:/app/kora.toml
      - ./signers.toml:/app/signers.toml
      - ./.env:/app/.env:ro
    environment:
      - RUST_LOG=info
      - REDIS_URL=redis://redis:6379
    env_file:
      - .env
    command:
      - sh
      - -c
      - "kora-rpc --config /app/kora.toml --signers-config /app/signers.toml"
    restart: unless-stopped
    depends_on:
      - redis

volumes:
  redis_data:
